# App
DEBUG=false
SECRET_KEY=change-me-generate-a-random-string
BASE_URL=http://localhost:8000

# Database
DATABASE_URL=sqlite+aiosqlite:///./statusping.db

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=alerts@statusping.app
SMTP_USE_TLS=true

# Monitoring
DEFAULT_CHECK_INTERVAL=300
DEFAULT_TIMEOUT=30
CONSECUTIVE_FAILURES_THRESHOLD=3
